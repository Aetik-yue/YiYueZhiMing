# 以越之名 · 代码编写规范

> 本文件供 AI 模型和开发者在编写代码前阅读。遵循本规范可以显著减少 bug。
> 项目：本地优先的情侣记忆 Android App（Kotlin + Jetpack Compose + Hilt + Room）。

---

## 一、项目结构与模块依赖（最重要）

### 1.1 多模块结构

```
app/            主模块：Application、MainActivity、Navigation、AndroidManifest
core/
  ui/           主题、动画、通用组件、Model 数据类、Routes
  data/         Room 数据库、DAO、Entity、Repository、Worker、播放器服务
feature/
  memories/     点滴（Home + AddMemory）
  album/        相册
  memo/         备忘录
  reminders/    日期提醒
  fortune/      运势（签运 + 塔罗）
  novel/        小说书架 + 阅读器
  music/        音乐播放器
  settings/     设置
  toolbox/      百宝箱（主入口）
```

### 1.2 依赖方向（严格遵守，禁止反向依赖）

```
app  →  feature:*  →  core:data  →  core:ui
```

- `core:ui` **不依赖**任何业务模块，是最底层。
- `core:data` 依赖 `core:ui`（因为 Mapper 用到 Compose 的 `Color`，Model 也在 core:ui）。
- feature 模块之间**禁止互相依赖**。共享的 UI 组件必须上提到 `core:ui`。
- 所有模块的 Kotlin **包名统一为 `com.example.yiyuezhiming.*`**（与模块名无关），这样移动文件不需要改 import。

### 1.3 代码放哪里（常见错误重灾区）

| 要写的东西 | 放在哪 |
|-----------|--------|
| 数据类 Model（Song、Book、Memory…） | `core:ui` 的 `model/` |
| Room Entity / DAO / Database | `core:data` 的 `data/local/` |
| Repository | `core:data` 的 `data/` |
| Hilt `@Module`（提供 Database/DAO/OkHttp） | `core:data` 的 `di/`（**不要放 app**，见 9.2） |
| 前台服务、Worker | `core:data` 的 `core/` |
| 多个 feature 共用的 Composable | `core:ui` 的 `ui/components/` |
| 单个 feature 的 Screen + ViewModel | 对应 `feature:xxx` |
| 路由定义 `Route` | `core:ui` 的 `navigation/Routes.kt` |
| 导航图 `AppNavGraph` | `app` 的 `navigation/` |

---

## 二、构建与版本

### 2.1 构建命令

```bash
JAVA_HOME="C:/Program Files/Zulu/zulu-21" \
java -cp "gradle/wrapper/gradle-wrapper.jar" \
  org.gradle.wrapper.GradleWrapperMain :app:assembleDebug --no-daemon
```

- Android SDK 路径在 `local.properties`（`D:\AppAndroid\Android\SDK`）。
- **写完代码必须实际构建验证**，不能只靠"看起来对"。

### 2.2 版本号规则

- `versionCode = major*100 + minor*10 + patch`（如 `1.1.2 → 112`）。
- `versionName` 为 `major.minor.patch`。
- 发版时 APK 放入 `version/<版本号>/以越之名.apk`，并写 `更新日志.md`。

---

## 三、Kotlin 编码规范

### 3.1 空安全（高频 bug 来源）

- **任何可能返回 null 的 Android API 必须处理 null**。典型例子：
  ```kotlin
  // ❌ 错误：MediaPlayer.create 可能返回 null，直接 .apply 会 NPE
  MediaPlayer.create(context, uri).apply { start() }

  // ✅ 正确
  MediaPlayer.create(context, uri)?.apply {
      setOnCompletionListener { it.release() }
      setOnErrorListener { p, _, _ -> p.release(); true }
      start()
  }
  ```
- 同理：`contentResolver.openInputStream(uri)`、`cursor.getColumnIndex()`（返回 -1 要判断）、`cursor.getString()` 等。

### 3.2 import 必须完整（最常见的编译错误）

- 使用任何类/函数前确认 import 存在。**以下是本项目踩过的坑**：
  - `coroutineContext.ensureActive()` 需要 `import kotlinx.coroutines.ensureActive`
  - `LaunchedEffect` 需要 `import androidx.compose.runtime.LaunchedEffect`
  - `withFrameMillis` 在 `androidx.compose.runtime` 包下，**不在** `kotlinx.coroutines`
- 引用资源 `R` 时注意模块：
  - `core:ui` 中用 `import com.example.yiyuezhiming.core.ui.R`
  - `core:data` 中用 `import com.example.yiyuezhiming.core.data.R`
  - 不能用 `com.example.yiyuezhiming.R`（那是 app 的，library 模块拿不到）

### 3.3 协程

- 长耗时循环（如批量抓取章节）要调用 `coroutineContext.ensureActive()` 支持取消。
- 阻塞 IO 用 `withContext(Dispatchers.IO)`；CPU 密集（如分页计算）用 `Dispatchers.Default`。
- 需要外部库的 `ListenableFuture.await()` 时，添加 `kotlinx-coroutines-guava` 依赖。

### 3.4 时区与日期（踩过的坑）

- **毫秒时间戳 ↔ LocalDate 必须带时区**：
  ```kotlin
  // ❌ 错误：忽略时区，UTC+8 下日期会偏移一天
  LocalDate.ofEpochDay(millis / 86_400_000L)

  // ✅ 正确
  Instant.ofEpochMilli(millis).atZone(ZoneId.systemDefault()).toLocalDate()

  // LocalDate → 毫秒
  date.atStartOfDay(ZoneId.systemDefault()).toInstant().toEpochMilli()
  ```

---

## 四、Jetpack Compose 规范

### 4.1 状态管理

- 界面状态统一用 `data class XxxState` + `MutableStateFlow` + `asStateFlow()`。
- 更新状态用 `_state.update { it.copy(...) }`，不要直接赋值整个对象。
- Screen 用 `val state by viewModel.state.collectAsState()` 收集。

### 4.2 副作用与动画

- 帧动画（如唱片旋转）用 `LaunchedEffect(条件) + withFrameMillis` 循环，暂停时能平滑续播：
  ```kotlin
  LaunchedEffect(isPlaying) {
      if (isPlaying) {
          var last = -1L
          while (isActive) {
              withFrameMillis { now ->
                  if (last >= 0) rotation += (now - last) * 速度
                  last = now
              }
          }
      }
  }
  ```
- 跨午夜刷新日期等定时逻辑用 `LaunchedEffect(Unit) { while(true){ ...; delay(60_000) } }`。

### 4.3 组件复用

- 被 **两个及以上** feature 使用的 Composable，必须移到 `core:ui/ui/components/`，不要让 feature 互相 import。
- 可点击元素统一用 `kawaiiClickable(pressedScale = ..., onClick = ...)`（带按压缩放），不要裸用 `clickable`。

---

## 五、ViewModel 规范

- 用 `@HiltViewModel` + `@Inject constructor`。
- 需要导航参数时用 `SavedStateHandle`：`checkNotNull(savedStateHandle["bookId"])`。
- 防重复点击/重复请求（TOCTOU）：标志位检查要**放进协程内部**，不能只在协程外查：
  ```kotlin
  fun draw() {
      if (state.value.isLoading) return        // 快速路径（可选）
      viewModelScope.launch {
          if (state.value.isLoading) return@launch  // ✅ 协程内再查一次，防并发
          _state.update { it.copy(isLoading = true) }
          ...
      }
  }
  ```
- `onCleared()` 里释放资源（取消轮询、移除 listener、释放 controller）。

---

## 六、数据层规范

### 6.1 Room 数据库（重要）

- **禁止使用 `fallbackToDestructiveMigration()`**（会清空用户数据）。
- 升级数据库必须：① `YiYueDatabase` 的 `version` +1；② 在 `AppModule` 写对应的 `Migration`；③ `addMigrations(...)` 注册。
- Entity 字段用 `NOT NULL DEFAULT` 时，Migration 的 `ALTER TABLE` 要一致。

### 6.2 Repository

- `@Singleton` + `@Inject constructor`，通过 DAO 访问数据。
- 对外暴露 `Flow` 用 `dao.observeXxx().map { it.toModel() }`。
- Entity ↔ Model 转换统一写在 `core:data` 的 `Mapper.kt`。

### 6.3 文件/网络/解析的健壮性

- 读取 ZIP（EPUB）时，`entry.size` 可能为 **-1（未知）**，不能直接当真实大小用，要读出字节后按实际大小判断；同时累计总量防 zip bomb。
- 文本编码检测顺序：`BOM → UTF-8 → GB18030 → UTF-16`，兜底 `GB18030`（中文用户 TXT 多为 GBK）。
- OkHttp 已配置 `callTimeout(120s)`，不要在业务代码里再写死超短超时。

---

## 七、Hilt 依赖注入规范（踩过的坑）

- **提供 Room Database 和 DAO 的 `@Module` 必须放在 `core:data`**，不能放 `app`。
  原因：Room 的 KSP 在 `core:data` 生成 `XxxDatabase_Impl`，Hilt Module 若放在 app 会因跨模块找不到生成类而报 `error.NonExistentClass`。
- `@HiltWorker` 的 Worker 放 `core:data`，通过 `@AssistedInject` 注入依赖。
- 前台服务用 `@AndroidEntryPoint`。

---

## 八、AndroidManifest 与权限

- Manifest 只在 **app 模块** 维护。
- 新增前台服务必须：① 声明 `<service android:foregroundServiceType="...">`；② 加 `FOREGROUND_SERVICE` + 对应类型的权限。
- 运行时权限（如 `READ_MEDIA_AUDIO`）用 `rememberLauncherForActivityResult(RequestPermission())`，并在 ViewModel 里做"已授权则直接加载"的判断。
- `usesCleartextTraffic="true"` 已开启（小说在线抓取需要 HTTP），不要随意关闭。

---

## 九、提交前自检清单

写完代码后，逐条检查：

- [ ] 实际执行了构建命令，**BUILD SUCCESS**（不是"应该能编译"）
- [ ] 所有新用到的类/函数都有 import（尤其 `ensureActive`、`LaunchedEffect`、`withFrameMillis`）
- [ ] 所有可能为 null 的返回值都做了空安全处理
- [ ] 涉及时间戳 ↔ 日期转换的地方带了时区
- [ ] 新增数据库字段写了 Migration，没有用 destructive
- [ ] 跨模块共享的组件放在了 `core:ui`，feature 之间没有互相 import
- [ ] Hilt Module（提供 DB/DAO）放在 `core:data`
- [ ] 循环/批量操作支持协程取消（`ensureActive`）
- [ ] 防重复操作的状态检查放在了协程内部

---

## 十、给 AI 模型的工作流建议

1. **先读本规范 + 相关模块的现有代码**，再动手。
2. 改动涉及多模块时，先确认依赖方向（第 1.2 节）。
3. 不确定某个 API 行为（如 `entry.size`、`withYear` 对 2/29 的处理）时，**查证后再写，不要臆测**。
4. 写完必须构建验证，构建报错先修 import 和空安全这两类最高频问题。
5. 修复 bug 时说明：原因、改法、影响范围。
